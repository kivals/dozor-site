import { typograph } from "@/lib/typography";
import type { Region } from "./types";

export const regions: Region[] = typograph([
  { id: "moscow", name: "Москва" },
  { id: "moscow-oblast", name: "Московская область" },
]);
